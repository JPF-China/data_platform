# Database Configuration
# PostgreSQL connection string (timezone-aware format)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/harbin_traffic

# Legacy connection info format (for compatibility)
DB_CONNINFO=dbname=harbin_traffic user=postgres host=localhost port=5432 password=postgres

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Business timezone for data processing
BUSINESS_TIMEZONE=Asia/Shanghai
