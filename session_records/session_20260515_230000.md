# Session: 速度/里程数据纠偏与部署流程重构

## Commit Message
fix(pipeline): 修复 segment 时间戳索引错位导致的 50.8% 速度异常，并精简部署流程

- 入仓层 segment 时间戳从 raw tms 切换为 matched times 数组
- 入仓层 avg_speed_kmh 裁剪至 (0,200] km/h
- deploy.sh 全量重写为 5 步清晰流程，删 _ensure_schema
- pipeline _step4_aggregate_all 自行加载 stats_schema.sql
- rebuild 流程重排序：lock→truncate→UNLOGGED→ingest→LOGGED→indexes→aggregate
- _recompute_segments_metrics 改为 50 万条分批计算 + 实时进度
- 去除 deploy.sh 自动注入 --pg-fast-mode
- start.sh 增加数据管线提示，SKIP_REGISTRY_CHECK 不阻断
- README 新增「日常启动」和「数据质量」章节
- spec/implementation_guide/project_context 同步速度过滤口径
- Makefile 新增 make rebuild 快捷入口

## 修改文件
backend/app/services/ingest_service.py   — B 时间戳修复
backend/app/etl/load_data.py             — A 速度裁剪 + 分批计算 + 流程重排 + schema 自加载
backend/app/services/stats_service.py    — 聚合层撤回速度过滤
infra/postgres/stats_schema.sql          — 聚合层撤回速度过滤
scripts/deploy.sh                        — 全量重写
scripts/start.sh                         — 管线提示 + registry 检查不阻断
Makefile                                 — 新增 rebuild
README.md / spec.md / project_context.md / implementation_guide.md — 文档同步

## 关键决策
- 速度过滤仅入仓层一处，聚合层只 IS NOT NULL
- UNLOGGED 模式不再自动开启（SET LOGGED 重写 45M 行 WAL 耗时与入仓相当）
- deploy.sh 不做 SQL 初始化，由 pipeline 自行保证

## 验证结果
- 全量 127 万 trip、4590 万 segment 重建通过
- max_spd=200.0, over_200=0, dur_under_1s=0
- 5 天均速 25-26 km/h，合理
