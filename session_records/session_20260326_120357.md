# Session: Workspace 与路由吸附对齐

## Commit Message
feat(workspace): 重构交互工作台并对齐 route/compare 吸附契约

## 本次完成
- 前端重构为左侧导航+右侧内容区，支持 Light/Dark 主题与模块切换。
- Heatmap 与 Route 地图图层职责分离，支持热力图图例/清空与 Route 地图选点。
- 后端 route/compare 增加自动吸附返回 snapped 点位，并同步 OpenAPI 示例与测试。

## 修改内容
- 更新 `backend/app/services/route_service.py`、`backend/app/services/route_search_service.py`、`backend/app/schemas.py`、`backend/app/api/routes.py`。
- 更新 `frontend/src/App.tsx`、`frontend/src/App.css`、`frontend/src/api.ts`、`frontend/src/App.test.tsx`。
- 修订 `backend/README.md`、`frontend/README.md`、`implementation_guide.md`。

## 关键决策
- 路由点位修正在后端服务层实现，前端只做展示与交互。
- Heatmap 子页仅展示热力图，Route 子页仅展示路线与起终点。

## 问题 / 风险
- 本机缺少 pytest 运行环境，后端回归未在当前环境执行。

## 下一步计划
- 在后端测试环境补跑 pytest 套件并补充 route/compare 响应样例文档。
