# Session: 全项目走查 + 前端视觉重构 + 大屏上线 + 文档同步

## Commit Message
feat: 前端视觉重构、driver_id/vehicle_id统一、大屏仪表盘、部署脚本完善

## 本次完成
- 走查 36 张表、21 个 API、8 个前端组件，确认文档与实现一致
- 修复 `_step4_aggregate_all()` buggy 路网代码，治理模块执行位置修正
- `driver_id` → `vehicle_id` 全链路统一（SQL/Python/前端/测试）
- 前端颜色令牌统一为 Tailwind `@theme`，删除 40 个 `!important` 暗色覆盖
- 间距标准化为 4px 体系，魔数全部替换
- 总览/热力/路径三组件重写，深色模式自适应
- 导航 5 组 → 3 组（分析/运营/系统）
- 真实 React 大屏组件（实时 API + MapLibre + 30s 自动刷新）
- 统一部署脚本 `scripts/deploy.sh`（--fresh/--auto/--module）
- 质量检查从 5 项增至 8 项

## 修改内容
- `backend/app/etl/load_data.py`：删除 buggy 路网代码 12 行
- `infra/postgres/stats_schema.sql`：driver_id→vehicle_id 迁移 + 8 项质量检查
- `frontend/src/App.css`：937→430 行，统一令牌 + 语义化组件
- `frontend/src/components/OverviewSection.tsx`：重写，dark-mode aware
- `frontend/src/components/HeatmapSection.tsx`：重写
- `frontend/src/components/RouteSection.tsx`：重写
- `frontend/src/components/BigScreen.tsx`：新增真实大屏组件
- `scripts/deploy.sh`：新增统一部署脚本
- 文档同步：README / frontend README / backend README / spec / impl_guide

## 关键决策
- 颜色令牌以 `index.css` Tailwind `@theme` 为唯一源，App.css 不再定义冲突变量
- 图表颜色保留硬编码值（SVG 不支持 CSS 变量），深色通过 `index.css` 覆盖
- 大屏用 Vite 多页构建（`bigscreen.html`），React 组件独立入口

## 问题 / 风险
- Stitch MCP 超时（~2min），但服务端已生成全部设计稿，可通过 `stitch_get_screen` 取回
- 前端截图使用 Puppeteer headless 获取，非手动截图

## 下一步计划
- 运维：`database_optimize` 模式补齐 VACUUM + 分区维护
- 体验：各组件加 skeleton loading / 空状态插图
- Stitch 设计稿可继续下载用于页面改版参考
