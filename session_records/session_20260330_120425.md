# Session: 修复热力图与路径展示并收口文档

## Commit Message
feat(pipeline): 增加 refresh 流程并修复路网映射与统计可见性

- 新增 `refresh` 模式，复用明细数据只刷新路网映射与统计，降低全量重建成本。
- 修复 `ingest_road_map` 早退导致映射不全问题，并改进 `table_row_stats` 关键表真实计数。
- 增加前端 `VITE_MAP_TILES` 配置与底图兜底，修正文档与实际运行方式不一致。

## 本次完成
- 修复热力图数据不完整与路径图层异常展示问题。
- 优化 Docker 启动行为，支持按数据库卷自动前端模式启动。
- 对齐主 README 与 backend/frontend/spec/context/implementation 文档。
- 更新 JLD2 数据地址为用户提供的 Google Drive 链接。

## 修改内容
- 后端：新增 `refresh` 编排；自动清理历史 stale `running` 记录。
- 后端：修复 `road_mapping_service` 映射早退；热力图几何聚合改为 road_segments 聚合。
- 后端：`table_row_stats` 增加 `ANALYZE` 与关键表 `COUNT(*)`。
- 前端：支持 `VITE_MAP_TILES` 多瓦片源配置，增加底图背景层兜底。
- 部署：docker-compose 服务增加 `restart: unless-stopped`。
- 脚本：`start.sh` 增加 `START_MODE=auto|full|frontend`。
- 文档：全面补齐运行模式、排障和地图瓦片配置。

## 关键决策
- 日常推荐 `refresh`，仅首次或需要重建明细时使用 `rebuild`。
- 保留 `auto` 启动策略，但明确“数据库卷存在”是有数据的代理条件。
- 关键展示表行数采用真实计数，避免新人误判构建失败。

## 问题 / 风险
- 受限网络下默认 OSM 瓦片可能不可达，需配置 `VITE_MAP_TILES`。
- 本地 `pytest` 依赖数据库角色环境，当前环境下未完整通过。

## 下一步计划
- 如需上线前收口，可补充一条针对 `refresh` 的后端回归测试。
- 可选：将瓦片服务地址改为团队统一可访问配置。
